pipeline {
    agent any
    stages {
        stage("clone"){
            steps{
               git 'https://github.com/kirankk6932/hello-world.git'
            }
        }
        stage("build"){
            steps{
               sh "mvn clean install"
            }
        }
    }
}
